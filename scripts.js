// JavaScript functionality for additional actions (if needed in the future)
// Currently, this file is empty as we only need to control the music with HTML

document.addEventListener("DOMContentLoaded", function() {
    // Music is set to autoplay and loop in the HTML file
});
